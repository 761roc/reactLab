import {
  InterviewEditorialPage,
  type EditorialFact,
  type EditorialSection,
} from '../../common/ui/InterviewEditorialPage';

const facts: EditorialFact[] = [
  { label: '条件类型', value: '决定某类型在什么条件下变成什么' },
  { label: '映射类型', value: '对对象每个属性批量做变换' },
  { label: '组合价值', value: '逐属性判断，再重建新结构' },
  { label: '典型用途', value: '字段过滤、字段重写、工具类型设计' },
];

const sections: EditorialSection[] = [
  {
    title: '1. 先分工：条件类型负责判断，映射类型负责遍历与重建',
    paragraphs: [
      '很多人会把条件类型和映射类型一起学乱，是因为没有先分清角色。最简单的说法是：条件类型像类型系统里的 if/else，决定某个类型在不同条件下走哪个分支；映射类型像类型系统里的 for 循环，决定如何遍历对象的每个属性并生成新结构。',
      '所以当它们配合使用时，本质上就是：对对象每个属性逐个判断，再决定这个属性在新类型里应该变成什么。',
      '只要这个职责分工先讲清，后面为什么它们经常一起出现就会非常自然。',
    ],
  },
  {
    title: '2. 条件类型擅长做类型分支判断',
    paragraphs: [
      '`T extends U ? X : Y` 这类写法，本质上是在判断 T 是否满足某个结构或关系，如果满足就变成 X，否则变成 Y。',
      '这意味着条件类型特别适合做类型分类，例如是不是函数、是不是 Promise、是不是联合中的某一类成员。',
      '所以可以把它理解成“类型层的判断逻辑”。',
    ],
  },
  {
    title: '3. 映射类型擅长做对象结构批量改造',
    paragraphs: [
      '映射类型通常基于 `[K in keyof T]` 这种形式，对对象 T 的每个属性统一处理。比如加 readonly、变 optional、做别名映射，都是它的强项。',
      '它本身不负责判断某个字段是什么类型，而是负责“把这些字段都重建一遍”。',
      '所以 mapped type 的核心价值，是对象结构批量改造。',
    ],
  },
  {
    title: '4. 两者结合后，最常见的价值是按属性类型做选择性改写',
    paragraphs: [
      '一旦把条件类型塞进映射类型里，就能做到：遍历对象每个属性，如果这个属性类型满足某条件，就替换成新类型，否则保持原样。',
      '例如把所有 string 字段改成 number、把所有函数字段过滤掉、把所有 Promise 字段拆包，这些都属于它们组合后的典型场景。',
      '这也是很多高级工具类型真正有价值的地方。',
    ],
    codeTitle: '按字段类型重写结构',
    code: `type StringToNumber<T> = {
  [K in keyof T]: T[K] extends string ? number : T[K];
};`,
  },
  {
    title: '5. 真正落到业务里，是做批量字段治理，而不是炫技',
    paragraphs: [
      '这组能力在业务里最常见的场景包括：从接口类型里挑选纯数据字段、按字段值类型自动生成表单 schema、批量把某类字段标记为只读、对组件 props 做统一变换等。',
      '也就是说，它们的价值不是让类型写得更花，而是把重复规则提升到类型层统一处理。',
      '回答这题时，如果你能从业务场景反推它们的意义，会比只讲语法强很多。',
    ],
  },
  {
    title: '6. 面试里怎样把这题答稳',
    paragraphs: [
      '先拆分条件类型和映射类型的职责；再说明它们组合后就是“遍历每个属性并按条件改写”；然后举一个字段重写示例；最后落到业务上的字段过滤和结构治理场景。',
      '一句话收尾可以这样说：条件类型和映射类型配合使用时，本质上是在对对象每个属性做“批量判断 + 批量重建”。',
    ],
  },
];

export default function TsTypeSystemConditionalMappedPage() {
  return (
    <InterviewEditorialPage
      archiveLabel="TypeScript Type System"
      company="面试-TypeScript 类型系统类"
      issue="Issue 04"
      title="条件类型和映射类型如何配合使用"
      strapline="一个负责判断，一个负责遍历；组合起来就是逐字段改写结构。"
      abstract="这道题不该只背两个语法定义，更重要的是理解它们合在一起时，如何成为对象类型批量治理的基础能力。"
      leadTitle="从职责拆分讲到批量字段改写，理解两者的组合价值"
      lead="如果先把条件类型和映射类型的职责分开，再解释它们如何合起来工作，这道题就会从抽象语法题变成非常清楚的结构计算题。"
      answerOutline={[
        '先分清条件类型和映射类型各自负责什么',
        '再讲它们组合后如何逐字段判断并重建',
        '然后给一个字段改写例子',
        '最后落到工具类型和业务场景',
      ]}
      quickAnswer="一句话答法：条件类型负责根据类型关系做分支判断，映射类型负责遍历对象属性并批量生成新结构；当两者配合时，本质上就是对对象每个属性逐个判断，再决定这个属性在新类型里应该变成什么，因此特别适合做字段过滤、字段重写和工具类型设计。"
      pullQuote="条件类型决定“怎么判断”，映射类型决定“改哪些字段”。"
      facts={facts}
      sections={sections}
      interviewTips={[
        '先分角色，再讲组合，这样最清楚。',
        '举一个按字段类型重写的例子会很有说服力。',
        '不要停留在语法层，最好补上业务层的批量结构治理。',
      ]}
      mistakes={[
        '把两个概念混成一个。',
        '只会写例子，不会解释为什么它们常常一起出现。',
        '回答停留在工具类型名字，没有落到结构改造价值。',
      ]}
      singleColumn
    />
  );
}
