import {
  InterviewEditorialPage,
  type EditorialFact,
  type EditorialSection,
} from '../../common/ui/InterviewEditorialPage';

const facts: EditorialFact[] = [
  { label: '核心对象', value: '画布结构、节点配置、交互行为、历史记录' },
  { label: '状态重点', value: '把持久状态和临时 UI 状态分层管理' },
  { label: '回放关键', value: '事件日志或命令栈，而不是简单快照堆叠' },
  { label: '答题角度', value: '可编辑、可撤销、可重做、可协作、可恢复' },
];

const sections: EditorialSection[] = [
  {
    title: '1. 先把问题拆开：拖拽编排页面不是单纯拖拽，而是“画布编辑器 + 状态机”',
    paragraphs: [
      '这类题很多人一上来就谈拖拽库选型，但真正的核心是状态设计。因为拖拽编排页面通常要支持节点新增、删除、嵌套、移动、连线、属性编辑、撤销重做，甚至还要支持回放和协作。',
      '所以我会先把问题抽象成两个部分：一个是画布本身的数据结构如何组织，另一个是编辑行为如何记录和重放。',
      '只要这两个部分没拆清，后面的技术选型都不稳。',
    ],
  },
  {
    title: '2. 状态管理上先分层：持久业务状态和临时交互状态不要混在一个树里',
    paragraphs: [
      '我通常会把状态拆成三层。第一层是 Schema 或文档状态，描述节点树、布局信息、属性配置、事件绑定，这部分是需要持久化和回放的核心数据；第二层是运行派生状态，比如选中节点、校验结果、画布缩放比、辅助线信息；第三层是临时交互状态，比如拖拽中的 hover、吸附线高亮、框选区域。',
      '只有第一层需要进入真正的历史记录和持久化链路，后两层更多是 UI 过程态。',
      '如果把所有状态都混在一个 store 里，不但性能差，撤销重做也会变得非常混乱。',
    ],
  },
  {
    title: '3. 回放不要只存快照，更稳的是命令模型或事件日志',
    paragraphs: [
      '很多人会说“每次编辑都存一个 JSON 快照”，这能工作，但成本很高，而且很难表达一次操作的语义。更稳的方案通常是命令模式或事件日志模式。',
      '例如插入节点、移动节点、修改属性、删除节点都可以抽象成命令。命令具备 do / undo 能力，就天然支持撤销重做；如果再持久化命令序列，就能支持操作回放。',
      '快照不是没用，但它更适合作为阶段性 checkpoint，用来加速恢复，而不是替代行为记录本身。',
    ],
  },
  {
    title: '4. 还要考虑性能：大画布场景里，局部更新和选择性订阅很重要',
    paragraphs: [
      '拖拽编排页面很容易出现大规模节点场景，这时候如果每次鼠标移动都让整棵树重渲染，体验会非常差。',
      '所以状态设计上要支持局部订阅，节点只关心自己的配置和选中态；拖拽中的高频状态要尽量压缩更新范围；必要时还要配合虚拟化、分层渲染和批量提交。',
      '面试里如果你主动补性能和渲染边界，说明你不是只会画结构图。',
    ],
  },
  {
    title: '5. 如果题目再深一点，回放还会延伸到协作、审计和故障恢复',
    paragraphs: [
      '当你有命令日志后，就不仅能做撤销重做，还可以做操作审计、时间点回放、异常恢复，甚至在多人协作时与 CRDT / OT 结合。',
      '这时候要考虑事件幂等、版本兼容、冲突合并和 checkpoint 清理策略。',
      '所以回放机制不是一个附加功能，它往往会决定整个编辑器底层的数据模型。',
    ],
  },
  {
    title: '6. 面试里怎样把拖拽编排题答稳',
    paragraphs: [
      '先说状态分层：文档状态、派生状态、临时交互状态；再说回放机制：命令栈或事件日志为主、快照为辅；然后补性能优化和多人协作扩展。',
      '一句话收尾可以这样说：拖拽编排页面的核心不是拖拽库，而是能否把可持久化的文档状态、可撤销的行为记录和高频交互过程态分层管理。',
    ],
  },
];

export default function ArchitectureInterviewDragLayoutStateReplayPage() {
  return (
    <InterviewEditorialPage
      archiveLabel="Architecture Interview"
      company="面试-架构 / 场景设计类"
      issue="Issue 02"
      title="如果让你设计一个拖拽编排页面，你会如何管理状态和回放"
      strapline="拖拽只是表面，真正难点是文档模型、命令记录和高频交互状态分层。"
      abstract="这道题重点考的是编辑器思维。高分回答通常不会停在拖拽库，而会从文档状态、命令栈、快照策略、性能边界和协作扩展来拆解。"
      leadTitle="把画布当成一份可编辑文档，而不是一堆零散组件状态"
      lead="只要你把拖拽编排页理解为“可编辑文档 + 命令历史”，很多设计就会自然顺下来，包括撤销重做、回放、恢复、审计和协作。"
      answerOutline={[
        '先拆画布文档模型和交互过程态',
        '再讲命令栈 / 事件日志的回放设计',
        '然后补快照、性能和选择性订阅',
        '最后扩展到协作、审计和恢复',
      ]}
      quickAnswer="一句话答法：设计拖拽编排页面时，我会先把状态分成文档状态、派生状态和临时交互状态。文档状态保存节点树、布局、属性和行为配置，是需要持久化和回放的核心；派生状态保存选中态、缩放、校验等；临时状态处理拖拽过程中的 hover、吸附线、框选等高频 UI。回放上我会优先用命令栈或事件日志做撤销重做和操作回放，快照只作为阶段性 checkpoint。"
      pullQuote="画布编辑器一旦没有明确的文档模型，后面所有撤销、回放、协作都会变成补丁工程。"
      facts={facts}
      sections={sections}
      interviewTips={[
        '不要把重点放在拖拽库名字上，核心是状态分层。',
        '命令栈 / 事件日志比“全量快照”更像成熟答案。',
        '顺手补性能和协作扩展，答案层次会明显更高。',
      ]}
      mistakes={[
        '把所有交互状态都塞进一个 store。',
        '只提快照，不提命令语义和撤销重做。',
        '完全忽略大画布场景下的渲染性能。',
      ]}
      singleColumn
    />
  );
}
