import {
  InterviewEditorialPage,
  type EditorialFact,
  type EditorialSection,
} from '../../common/ui/InterviewEditorialPage';

const facts: EditorialFact[] = [
  { label: '闭包本质', value: '函数保留了对其词法环境中变量的引用' },
  { label: '旧值问题', value: '读到的是被捕获时那份环境，不是自动最新值' },
  { label: '内存问题', value: '只要还有引用，相关环境就可能无法释放' },
  { label: '答题重点', value: '区分闭包正常能力与错误使用带来的副作用' },
];

const sections: EditorialSection[] = [
  {
    title: '1. 闭包本身不是问题，它是 JavaScript 的正常能力',
    paragraphs: [
      '闭包最准确的理解是：一个函数即使脱离了原始调用位置执行，仍然能够访问定义它时所在作用域里的变量。也就是说，函数会“记住”它的词法环境。',
      '这本身是一种非常有价值的能力，比如做私有变量、工厂函数、函数柯里化、事件处理器时都离不开它。',
      '所以回答这道题时，最好先澄清：闭包不是 bug，问题在于闭包保留环境后，可能带来旧值感知或引用无法释放的副作用。',
    ],
  },
  {
    title: '2. “拿到旧值”本质上是拿到了旧作用域里的变量快照关系',
    paragraphs: [
      '所谓闭包导致“拿到旧值”，本质上不是闭包把值复制了一份，而是你当前执行的这段函数，仍然在读取某次创建时对应的那份作用域链里的变量关系。',
      '在 React、事件回调、定时器、异步回调里，这个问题特别常见：外层状态已经变了，但某个旧回调还持有旧环境，因此读到的是旧版本变量。',
      '所以更准确的说法是：闭包读到的不是“程序全局最新值”，而是它所捕获那条词法作用域链里的值。',
    ],
    codeTitle: '旧闭包读旧环境',
    code: `function createCounter() {
  let count = 0;

  return () => {
    console.log(count);
  };
}`,
  },
  {
    title: '3. 内存不释放的根因，是闭包把本该可回收的环境继续挂住了',
    paragraphs: [
      'JavaScript 的垃圾回收原则很简单：对象或环境只要还有可达引用，就不能回收。闭包会保留对外层作用域的引用，因此如果这个闭包本身长期存在，外层环境里被引用到的变量也可能一直存活。',
      '这就是为什么一些事件监听、定时器回调、缓存函数、长生命周期对象，如果闭包里不小心引用了大对象或 DOM 节点，就可能导致内存无法及时释放。',
      '所以闭包造成的内存问题，本质上不是“闭包神秘泄漏”，而是“引用链没有断开”。',
    ],
  },
  {
    title: '4. 真正的风险场景，往往是异步回调、监听器、循环引用和大对象捕获',
    paragraphs: [
      '最常见的几类问题场景包括：定时器回调长期引用旧状态、事件监听器解绑不及时、缓存函数把大对象一直保存在闭包里、以及某些场景下闭包引用 DOM 节点导致页面卸载后内存仍被挂住。',
      '这类问题的共同点不是“用了闭包”，而是“闭包生命周期长于你以为的那段业务逻辑”。',
      '所以定位时重点不是看到闭包就害怕，而是看它引用了什么、活多久、何时能释放。',
    ],
  },
  {
    title: '5. 面试里怎样把闭包的风险讲完整',
    paragraphs: [
      '先讲闭包本身是函数保留词法环境的正常能力；再解释为什么会出现旧值问题；然后说明只要闭包继续可达，外层环境就可能不被回收，从而产生内存滞留；最后举监听器、定时器和 React 回调这些高频场景。',
      '一句话收尾可以这样说：闭包的问题从来不是“它存在”，而是“它把哪段环境保留了多久，以及这是否符合你的预期”。',
    ],
  },
];

export default function BrowserJsInterviewClosurePage() {
  return (
    <InterviewEditorialPage
      archiveLabel="Browser / Async / JS Interview"
      company="面试-浏览器 / 异步 / JS 基础类"
      issue="Issue 03"
      title="闭包为什么会导致“拿到旧值”或内存不释放"
      strapline="闭包不是问题，问题在于它保留了哪段环境、保留了多久。"
      abstract="这道题高分回答的关键，是先把闭包放回“词法环境引用”这件事上，再解释为什么旧值和内存滞留都是这个机制的自然结果。"
      leadTitle="从词法环境引用出发，解释旧值与内存滞留两类现象"
      lead="如果你只说“闭包会缓存变量”，答案会不够准确。更完整的说法应该强调：闭包让函数持续引用定义时的词法环境，因此既可能读到旧环境中的值，也可能让某些原本可回收的对象继续存活。"
      answerOutline={[
        '先讲闭包本质是保留词法环境引用',
        '再讲为什么会出现旧值问题',
        '然后讲为什么会导致环境不被回收',
        '最后落到定时器、监听器和 React 回调场景',
      ]}
      quickAnswer="一句话答法：闭包之所以会导致“拿到旧值”或内存不释放，是因为函数会持续引用定义它时所在的词法环境。这样一来，异步回调或旧函数实例可能继续读取旧环境里的变量；同时只要这个闭包还可达，它引用到的外层环境和对象也可能无法被垃圾回收及时释放。"
      pullQuote="闭包不会主动制造问题，它只是忠实地把环境留住了。"
      facts={facts}
      sections={sections}
      interviewTips={[
        '先说闭包是正常能力，再说副作用，层次会更好。',
        '“旧值”要解释成旧词法环境，而不是简单说变量被复制。',
        '内存问题一定要落到“引用链未断开”，不要说得太玄。',
      ]}
      mistakes={[
        '把闭包本身说成 bug。',
        '把旧值问题解释成值被拷贝死了。',
        '把内存问题神秘化，不回到引用和 GC 原理。',
      ]}
      singleColumn
    />
  );
}
